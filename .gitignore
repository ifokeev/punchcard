# Local planning artifacts (specs, plans) — kept out of the repo
docs/superpowers/

# macOS
.DS_Store

# Build artifacts
/punch
/punchcard
/dist/

# Runtime data (created by `punch serve`)
/tasks.json
/memory.json
/control.json
/artifacts/

# Third-party agent skills installed via `npx skills` (not part of punchcard)
.agents/
